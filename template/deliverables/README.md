# Deliverables

Derived artifacts built FROM experiments — talks, manuscripts, reports, posters.
One folder per deliverable: `deliverables/<slug>/`, each with a README stating its purpose and
**which experiments it draws on** (so a figure in a talk is traceable to the run that made it).
Not a run (no hypothesis/metrics) — that's what `experiments/` is for.
