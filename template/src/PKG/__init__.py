"""{{PROJECT}} — reusable library code.

Import these from experiments and notebooks instead of re-implementing them:
    from {{PKG}} import config, provenance, tracking
See AGENTS.md for the project map. Add new shared utilities to this package, not to scripts.
"""
from . import config, provenance, tracking  # noqa: F401
